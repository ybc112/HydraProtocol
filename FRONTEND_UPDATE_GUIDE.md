# 前端批次计算功能集成指南

## 📊 **当前状态**

根据你的截图，前端 Compute 页面还是旧版界面，只支持：
- ✅ Average 电路（3个数据点）
- ✅ Threshold 电路（10个数据点）
- ❌ **批次计算功能尚未集成**

---

## 🎯 **需要添加的功能**

### **选项 1：添加计算模式切换（推荐）**

在现有页面添加一个模式选择器：

```
┌─────────────────────────────────────┐
│  Computation Mode:                  │
│  ○ Quick Mode (3-10 data points)   │
│  ● Batch Mode (up to 10,000 points)│
└─────────────────────────────────────┘
```

**优点**：
- 保持向后兼容
- 用户可以根据数据量选择模式
- 界面统一

### **选项 2：创建独立的批次计算页面（简单）**

创建新路由 `/compute/batch`，专门用于批次计算。

**优点**：
- 不影响现有功能
- 代码隔离，易于维护
- 快速实现

---

## 🚀 **快速实现方案（选项 2）**

我建议先用选项 2，创建一个独立的批次计算页面，这样最快最安全。

### **步骤 1：创建批次计算页面**

创建文件：`frontend/src/app/compute/batch/page.tsx`

这个页面将使用我们已经创建好的：
- `useBatchComputation` Hook
- `batch-processor.ts` 工具函数
- 批次电路文件

### **步骤 2：在导航栏添加入口**

在 Header 组件添加 "Batch Compute" 链接。

### **步骤 3：测试**

上传大数据集（100+ 行），测试批次计算功能。

---

## 📝 **我现在要做什么？**

我可以帮你：

### **选择 A：创建独立批次计算页面（推荐，快速）**
- 创建 `/compute/batch` 页面
- 添加导航链接
- 保持现有功能不变
- **预计时间：10分钟**

### **选择 B：升级现有 Compute 页面（复杂）**
- 添加模式切换器
- 重构现有代码
- 需要更多测试
- **预计时间：30分钟**

---

## 🤔 **你想要哪个方案？**

**请告诉我你的选择：**
- 回复 "A" - 我会创建独立的批次计算页面
- 回复 "B" - 我会升级现有页面添加模式切换

或者，如果你想先看看批次计算页面的效果，我可以直接创建给你看！
