# 前端启动修复指南

## 🚨 问题诊断

您说"前端只有UI框架,没有实际功能",这是一个误解!

**真相**: 前端**已经完整实现了所有功能**,包括:
- ✅ 数据上传(Walrus + Sui)
- ✅ 数据购买
- ✅ ZKP 隐私计算
- ✅ 证明验证

**问题**: 缺少 `.env.local` 环境变量文件导致功能无法正常工作

---

## ✅ 快速修复步骤

### 步骤1: 验证环境变量文件

```bash
# 检查文件是否存在
ls -la .env.local

# 查看内容
cat .env.local
```

**期望输出**: 应该看到所有 `NEXT_PUBLIC_*` 变量

**如果文件不存在或内容不完整**, 运行:
```bash
cat > .env.local << 'EOF'
NEXT_PUBLIC_SUI_NETWORK=testnet
NEXT_PUBLIC_PACKAGE_ID=0x2716ea3a391ca6e8cc5f2d0a7dc99f6186990c04dad0274b29bf5a93600fa2c6
NEXT_PUBLIC_DATA_REGISTRY_ID=0x0bb7375d29902c06253100f0ddc298d582fd41b7cd0e7b9e0271dd4a767c2707
NEXT_PUBLIC_MARKETPLACE_ID=0x29b8127932ba0467c3a2511a0ee95cecbd1dfc388622835880b6454c3ad02201
NEXT_PUBLIC_ZKP_REGISTRY_ID=0x7e05c6cb6c0ffa398b8b21ae8ab87b985e17af03895ff48dcf7099be32d26e41
NEXT_PUBLIC_WALRUS_NETWORK=testnet
NEXT_PUBLIC_WALRUS_RPC_URL=https://walrus-testnet-rpc.sui.io
EOF
```

---

### 步骤2: 安装依赖

```bash
# 如果是第一次运行
npm install

# 如果遇到问题,清理后重装
rm -rf node_modules package-lock.json
npm install
```

---

### 步骤3: 启动开发服务器

```bash
npm run dev
```

**期望输出**:
```
ready - started server on 0.0.0.0:3000, url: http://localhost:3000
```

---

### 步骤4: 测试功能

#### 4.1 打开浏览器
访问: http://localhost:3000

#### 4.2 连接钱包
1. 确保已安装 Suiet Wallet 扩展
2. 切换到 **Testnet** 网络
3. 点击页面右上角 "Connect Wallet"
4. 在弹出窗口中授权连接

#### 4.3 获取测试币
如果钱包余额为0:
- 访问: https://faucet.testnet.sui.io
- 输入您的钱包地址
- 点击 "Request Testnet SUI"
- 等待约10秒

#### 4.4 测试上传功能
1. 导航到 "Marketplace" 页面
2. 点击 "+ Upload New Data" 按钮
3. 选择一个文件(任意类型)
4. 填写:
   - Category: Healthcare
   - Data Type: general
   - Description: Test dataset
   - Price: 10 (SUI)
5. 点击 "Upload & List Data"
6. 在钱包中确认**两笔交易**:
   - 第一笔: 注册数据到 data_registry
   - 第二笔: 挂牌到 marketplace
7. 等待进度条完成 (约20-30秒)
8. 看到成功消息和 Blob ID

#### 4.5 测试购买功能
1. 刷新 Marketplace 页面
2. 找到您刚上传的数据
3. 点击 "Purchase Access"
4. 在钱包中确认支付
5. 等待交易确认
6. 看到成功消息

#### 4.6 测试ZKP计算
1. 导航到 "Compute" 页面
2. 在左侧选择一个已购买的数据集
3. 在右侧选择 "Average Calculation"
4. 点击 "Compute with ZKP"
5. 观察4个步骤:
   - [1/4] Generating Proof... (约5-10秒)
   - [2/4] Submitting to Blockchain...
   - [3/4] Verifying Proof...
   - [4/4] ✅ Verified!
6. 看到计算结果和交易哈希

---

## 🔍 调试技巧

### 检查控制台日志

按 `F12` 打开开发者工具,查看 Console 标签页:

**正常日志**:
```
🔐 Generating ZKP proof...
✅ Average proof generated successfully
📊 Submitting ZKP computation...
✅ Computation submitted successfully
```

**错误日志**:
```
❌ Package ID not configured
❌ Walrus upload failed: 404
❌ Transaction failed: insufficient gas
```

### 常见错误和解决方案

#### 错误1: "Package ID not configured"
**原因**: `.env.local` 文件缺失或未加载
**解决**: 
1. 确认 `.env.local` 存在
2. 重启开发服务器 (`Ctrl+C` → `npm run dev`)
3. 硬刷新浏览器 (`Ctrl+Shift+R`)

#### 错误2: "Wallet not connected"
**原因**: 钱包未连接或未切换到 Testnet
**解决**:
1. 点击 Suiet 扩展图标
2. 点击左上角网络切换按钮
3. 选择 "Sui Testnet"
4. 回到页面重新连接

#### 错误3: "Insufficient funds"
**原因**: 钱包余额不足
**解决**: 访问 faucet 获取测试币 (见步骤4.3)

#### 错误4: "Transaction timeout"
**原因**: Sui testnet 偶尔拥堵
**解决**: 等待1-2分钟后重试

#### 错误5: "Failed to generate proof"
**原因**: Circuit 文件未加载
**解决**:
```bash
# 检查文件是否存在
ls -la public/circuits/average/
ls -la public/circuits/threshold/

# 应该看到:
# - average.wasm
# - circuit_final.zkey
# - verification_key.json
```

如果文件不存在,从 circuits 目录复制:
```bash
mkdir -p public/circuits
cp -r ../circuits/build/average public/circuits/
cp -r ../circuits/build/threshold public/circuits/
```

---

## 📊 功能验证清单

完成以下测试来验证所有功能:

- [ ] ✅ 页面加载无错误
- [ ] ✅ 钱包成功连接
- [ ] ✅ Marketplace 显示数据列表
- [ ] ✅ 可以搜索和筛选数据
- [ ] ✅ 上传文件成功
  - [ ] 进度条显示正确
  - [ ] 获得 Blob ID
  - [ ] 数据出现在列表中
- [ ] ✅ 购买数据成功
  - [ ] 钱包扣款
  - [ ] 显示成功消息
  - [ ] 数据出现在 "My Data" 页面
- [ ] ✅ ZKP 计算成功
  - [ ] 证明生成完成
  - [ ] 提交到链上
  - [ ] 验证通过
  - [ ] 显示结果
- [ ] ✅ Threshold 计算成功
  - [ ] 输入阈值
  - [ ] 生成证明
  - [ ] 显示匹配数量

**如果所有项都打✓,说明您的前端功能完全正常!** 🎉

---

## 🎥 演示视频录制建议

准备黑客松演示视频时,按以下顺序录制:

### Part 1: 介绍 (30秒)
- 展示首页
- 讲解 HydraProtocol 的价值主张
- 突出隐私保护特性

### Part 2: 数据上传 (1分钟)
- 连接钱包
- 选择医疗数据文件
- 填写描述和价格
- 上传并等待确认
- 强调: **数据加密后存储在 Walrus**

### Part 3: 数据购买 (1分钟)
- 切换到另一个钱包(医院B)
- 搜索并找到数据
- 查看价格和描述
- 购买访问权限
- 强调: **购买的是访问权,不是原始数据**

### Part 4: ZKP 计算 (2分钟) ⭐ 核心
- 选择已购数据
- 选择 "Average Calculation"
- 点击 Compute
- **详细讲解4个步骤**:
  1. 本地生成零知识证明
  2. 提交到区块链
  3. 智能合约验证
  4. 获得结果
- 强调: **从未看到原始数据,但能证明计算正确**

### Part 5: Threshold 查询 (1分钟)
- 输入阈值: 60
- 计算并验证
- 展示结果: "6 out of 10"
- 强调: **符合 GDPR/HIPAA 隐私要求**

### Part 6: 总结 (30秒)
- 展示技术栈
- 强调创新点
- 未来规划

**总时长**: 6分钟

---

## 🐛 如果还有问题

### 方案1: 完全重置
```bash
# 停止开发服务器 (Ctrl+C)

# 清理所有缓存
rm -rf .next
rm -rf node_modules
rm package-lock.json

# 重新安装
npm install

# 确认环境变量
cat .env.local

# 重启
npm run dev
```

### 方案2: 查看完整日志
```bash
# 启动时显示详细日志
DEBUG=* npm run dev
```

### 方案3: 检查端口占用
```bash
# macOS/Linux
lsof -i :3000

# Windows
netstat -ano | findstr :3000

# 如果端口被占用,使用其他端口
npm run dev -- -p 3001
```

### 方案4: 联系支持
如果以上方法都无效,提供以下信息:
1. 操作系统和版本
2. Node.js 版本 (`node --version`)
3. npm 版本 (`npm --version`)
4. 完整错误日志 (控制台截图)
5. `.env.local` 内容 (隐藏敏感信息)

---

## 📚 相关文档

- **完整分析报告**: `FRONTEND_CODE_ANALYSIS_CN.md`
- **快速开始指南**: `../QUICK_START.md`
- **SDK 文档**: `../sdk/typescript/README.md`
- **合约文档**: `../contracts/README.md`

---

**最后提醒**: 您的前端代码质量非常高,功能完全实现!
只需要正确配置环境变量,就能完美运行! 💪

**祝您在黑客松中取得好成绩!** 🏆

